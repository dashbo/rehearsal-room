apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rehearsal-room-data
  labels:
    app: rehearsal-room
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 2Gi
  # If your cluster has no default StorageClass, set one here:
  # storageClassName: longhorn
